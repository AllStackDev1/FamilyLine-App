import { FC } from 'react'

import { Checkbox, CheckboxGroup, Stack, Text } from '@chakra-ui/react'

const FormCheckbox: FC = () => {
  return (
    <Checkbox size={'md'}>
      {/* <Text fontSize={{ '2xl': 'md' }}>{title}</Text> */}
    </Checkbox>
  )
}

export default FormCheckbox
